//function takes a string and target and tests if string ends with target word

function confirmEnding(str, target) {
  // "Never give up and good luck will find you."
  // -- Falcor
  if(str.endsWith(target)) {
    return true;
  }
  else {
    return false;
  }
} 

confirmEnding("Bastian", "n");

